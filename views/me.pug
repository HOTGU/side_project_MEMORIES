extends layout.pug

block content
    .container
        .me
            if user.avatar
                img(src=user.avatar).imgAvatar
            else
                img(src="/static/images/no_user.jpg").imgAvatar
            h1.me__name=user.name

            a(href="/me/update")
                button.btn.updateBtn 수정하기