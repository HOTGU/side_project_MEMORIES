extends layout.pug

block content
    form(action="/memory/upload", method="POST" enctype="multipart/form-data")
        input(type="file" name="thumbnail")
        input(type="text" name="title" placeholder="제목")
        textarea(name="description" placeholder="본문")
        button 생성